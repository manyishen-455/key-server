{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/verify",
            "methods": ["POST"],
            "dest": "/server.js"
        }
    ]
}